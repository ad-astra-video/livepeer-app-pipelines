# WHIP and WHEP endpoint hosts
# Format: hostname:port (port is optional)
# The application will construct full URLs using these hosts

# WHIP endpoint host (for streaming/publishing)
VITE_WHIP_HOST=localhost:5937

# WHEP endpoint host (for viewing/playback)
VITE_WHEP_HOST=localhost:8890

# Kafka SSE endpoint host (for event logs)
VITE_KAFKA_HOST=localhost:7114